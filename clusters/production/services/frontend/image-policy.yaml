apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: frontend
  namespace: flux-system
  labels:
    service: frontend
spec:
  imageRepositoryRef:
    name: frontend
  digestReflectionPolicy: Always
  interval: 10m
  filterTags:
    pattern: '^latest$'
  policy:
    alphabetical:
      order: asc
