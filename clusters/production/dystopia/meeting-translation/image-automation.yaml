apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageUpdateAutomation
metadata:
  name: meeting-translation
  namespace: flux-system
spec:
  interval: 30m
  sourceRef:
    kind: GitRepository
    name: monorepo
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: panicboat@gmail.com
        name: panicboat
      messageTemplate: |
        chore(meeting-translation): bump image to {{range .Changed.Changes}}{{ println .NewValue }}{{end}}
    push:
      branch: main
  update:
    path: ./dystopia/meeting-translation/kubernetes/overlays/production
    strategy: Setters
