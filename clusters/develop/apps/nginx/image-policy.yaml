---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: nginx
  namespace: flux-system
  labels:
    service: nginx
spec:
  imageRepositoryRef:
    name: nginx
  policy:
    semver:
      range: ">=1.0.0"
