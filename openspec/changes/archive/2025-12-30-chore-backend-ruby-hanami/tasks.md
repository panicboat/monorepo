# Tasks: Backend Switch (Hanami)

- [x] Initialize Hanami <!-- id: 1 -->
    - Run `hanami new .` in `services/monolith`.
    - Setup `Gemfile` and `config.ru`.

- [x] Setup gRPC Environment <!-- id: 2 -->
    - Install `protoc` and plugins.
    - Add `gruf` gem.
    - Define initial `.proto` for Identity Service.
