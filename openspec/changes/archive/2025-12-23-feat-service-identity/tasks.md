# Tasks: Identity Service Setup

- [x] `services/monolith/src/internal/identity` ディレクトリを作成する (Create identity module directory)
- [x] `services/monolith/src/api/proto/v1/identity.proto` を作成し、gRPC 定義を記述する (Define gRPC proto)
- [x] データベーススキーマ (`users` テーブル等) のマイグレーションファイルを作成する (Create DB migration)
- [x] 簡易的なサーバー実装を行い、起動確認ができる状態にする (Implement basic server)
