# Tasks: App Shell & MSW Setup

- [x] `web/packages/ui` を作成し、React + Tailwind の共有ライブラリとして設定する (Setup UI package)
- [x] `demo` アプリから `Button` や `Layout` などの基本コンポーネントを移植する (Port base components)
- [x] `web/apps/shell` に Next.js アプリケーションをスキャフォールドし、`ui` パッケージを依存に追加する (Scaffold Next.js shell with UI lib)
- [x] `msw` パッケージをインストールし、ブラウザ/サーバー両用の設定を行う (Install and configure MSW)
- [x] `web/apps/shell/src/mocks/handlers/identity.ts` を作成し、`IdentityService` のレスポンスを定義する (Implement Identity mock handlers)
- [x] 動作確認用のページ (簡単なログイン画面等) を実装し、UIコンポーネントとモックAPIの連携をデモする (Implement verification page)
