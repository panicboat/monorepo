# Tasks: Structure Setup

- [x] `web/` ディレクトリを作成し、Node.js プロジェクトのルートとして初期化する (Initialize web directory)
- [x] `services/monolith/src` ディレクトリを作成する (Create monolith src directory according to convention)
- [x] ルートの `package.json` を `web/` に移動、または適切に分離する (Move/Refactor root package.json)
- [x] `web/apps` と `web/packages` ディレクトリを作成する (Scaffold web subdirectories)
