# Tasks

- [x] Create `services/docs/workspace/docker-compose.yaml` <!-- id: 0 -->
    - Define a service that builds the Dockerfile and exposes port 3000 (if dev) or 80 (if prod).
- [x] Verify/Fix `services/docs/workspace/Dockerfile` <!-- id: 1 -->
    - Ensure build dependencies are satisfied.
- [x] Verify local execution <!-- id: 2 -->
    - Run `docker-compose up`.
