{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:base"],
  "timezone": "Asia/Tokyo",
  "labels": ["dependencies"],
  "assigneesFromCodeOwners": true,
  "reviewersFromCodeOwners": true,

  "automerge": true,
  "automergeType": "pr",
  "automergeStrategy": "squash",
  "platformAutomerge": true,

  "schedule": ["before 4am on weekday"],
  "minimumReleaseAge": "1 day",

  "packageRules": [
    {
      "description": "🚫 Production environments - manual review only",
      "matchPaths": ["**/production/**", "**/*production*/**"],
      "automerge": false,
      "addLabels": ["⚠️ production", "manual-review"]
    },

    {
      "description": "🚫 Major updates - manual review only",
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "addLabels": ["📈 major-update", "manual-review"]
    },

    {
      "description": "✅ Everything else - auto-merge",
      "matchPackagePatterns": ["*"],
      "automerge": true,
      "addLabels": ["🤖 auto-merge"],
      "groupName": "{{packageFileDir}} dependencies"
    }
  ],

  "prConcurrentLimit": 15,
  "prHourlyLimit": 3,

  "branchPrefix": "renovate/",

  "commitMessagePrefix": "⬆️",
  "commitMessageAction": "Update",
  "commitMessageTopic": "{{depName}}",
  "commitMessageExtra": "to {{newVersion}}",

  "prTitle": "⬆️ {{commitMessageAction}} {{commitMessageTopic}} {{commitMessageExtra}}",
  "prBodyTemplate": "This PR updates {{depName}} from {{currentVersion}} to {{newVersion}} in `{{packageFileDir}}`.\n\n### Auto-merge Status\n{{#if (equals automerge true)}}✅ **Auto-merge enabled** - This PR will be automatically merged when CI passes{{else}}❌ **Manual review required** - This PR requires manual approval{{/if}}\n\n### Change Type\n{{#if (equals updateType 'major')}}📈 **Major Update** - Breaking changes possible{{else if (equals updateType 'minor')}}✨ **Minor Update** - New features added{{else}}🩹 **Patch Update** - Bug fixes only{{/if}}\n\n### Service Impact\n**Affected Path**: `{{packageFileDir}}`\n**Manager**: {{manager}}\n**Update Type**: {{updateType}}\n\n### Release Notes\n{{#if hasReleaseNotes}}{{releaseNotes}}{{else}}No release notes available{{/if}}\n\n---\n*This PR was created by Renovate Bot using pattern-based auto-detection*"
}
